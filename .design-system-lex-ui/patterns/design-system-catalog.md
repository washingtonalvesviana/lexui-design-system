# Catálogo navegável

Referência: `/design-system`.

É o índice humano do LexUI dentro do SaaS de demonstração. Agrupa componentes por intenção e liga aos patterns oficiais. O Storybook continua sendo a referência isolada de estados e APIs; a rota `/design-system` demonstra descoberta e navegação no contexto de produto.
