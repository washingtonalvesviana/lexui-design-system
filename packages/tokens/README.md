# @lexui/tokens

Tokens semânticos, temas claro/escuro e fundações responsivas do LexUI.

```ts
import "@lexui/tokens/theme.css"
```

Personalize a marca sobrescrevendo `--lex-brand-blue` e `--lex-brand-orange` em uma camada posterior do CSS.
